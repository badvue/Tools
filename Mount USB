# Find USB Drive
sudo fdisk -l
# Create Mount Point
sudo mkdir /media/usb-drive
# Mount USB Drive, replace "sdc1" with actual device name
sudo mount /dev/sdc1 /media/usb-drive/
# Unmount USB
sudo umount /media/usb-drive
